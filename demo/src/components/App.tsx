import {h, Component} from "preact";

export default class App extends Component<Record<string, never>, Record<string, never>> {
    render() {
        return <div>
            <h1>Hello World!</h1>
        </div>;
    }
}